# Rails API, including its versioned API path.
NEXT_PUBLIC_API_BASE_URL=https://api.alpinegrooveguide.com/api/v1

# Public AzuraCast stream URL. Leave blank until streaming is configured.
NEXT_PUBLIC_STREAM_URL=
